/**
 * The view id this package owns. One module so the rail button's pressed
 * state, its click, and the keyed `view` registration cannot drift apart.
 */

/** Frame view id of the full-bleed agent-roster surface. */
export const AGENTS_VIEW = 'agents'

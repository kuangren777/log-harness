/**
 * @deepseek-ai/dsh-office-univer — Univer office-document capability for the
 * harness: a Service Provider over an out-of-process Univer Gateway, the
 * model-facing `univer_*` tool Consumer, the bundled Univer skills, and the
 * browser API plus reverse proxy the Web client reads.
 *
 * Vendored from `dsh-univer-office`; the fork point, the pinned artifact
 * release, and every local modification are recorded in
 * [docs/office-univer-upstream.md](../../../docs/office-univer-upstream.md).
 */

export * from './host/index.ts'
